# Hero background photo(s)

The background image for the homepage hero section. Recommended:
landscape orientation, at least 2000px wide — it fills the full viewport
width and height, including a slow parallax zoom on scroll.

To use one, update the `background: url(...)` value on `.hero-bg` in
`src/index.css` to `/images/hero/<filename>`.
